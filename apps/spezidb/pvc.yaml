apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spezidb-storage
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
