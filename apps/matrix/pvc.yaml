apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-data
  namespace: default
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
